# Copy to .env and fill in. The .env file is gitignored.
#
# No secrets are currently required. The homepage globe is rendered with
# globe.gl (three.js) and self-hosted textures, so there is no Cesium ion
# token or tile quota to manage.
